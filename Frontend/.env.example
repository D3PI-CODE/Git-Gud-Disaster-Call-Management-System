# Copy to .env for local dev. In Vercel, add the same keys under Project → Settings → Environment Variables.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
# Local backend (uvicorn). Production Vercel uses Railway:
# https://git-gud-disaster-call-management-system-production.up.railway.app
VITE_API_URL=http://localhost:8000
